# 반려동물 피부질환 분류 AI 모델 분석 보고서

> 프로젝트: `Arf-ly`  
> 작성일: 2026-06-01

---

## 1. 프로젝트 개요

반려동물(강아지, 고양이)의 피부 이미지를 입력받아 **피부질환 유/무 및 질환 종류를 자동으로 분류**하는 딥러닝 기반 분류 시스템이다.

### 1.1 전체 파이프라인

```
원천 이미지 (JSON 어노테이션 포함)
        ↓  polygon2traindata.py (Bounding Box 크롭)
Bounding Box 이미지
        ↓  make_tfrecord.py (TFRecord 변환)
TFRecord 데이터셋
        ↓  *_train.py (모델 학습)
학습된 모델 (Inception V4)
        ↓  *_test.py (평가)
분류 결과 + Confusion Matrix
```

---

## 2. 데이터 구조

### 2.1 레이블 체계

| 레이블 코드 | 정수 레이블 | 의미                  |
| ----------- | ----------- | --------------------- |
| A1          | 1           | 피부질환 1번 (유증상) |
| A2          | 2           | 피부질환 2번 (유증상) |
| A3          | 3           | 피부질환 3번 (유증상) |
| A4          | 4           | 피부질환 4번 (유증상) |
| A5          | 5           | 피부질환 5번 (유증상) |
| A6          | 6           | 피부질환 6번 (유증상) |
| 무증상      | 7           | 정상 (무증상)         |

### 2.2 데이터 디렉토리 구조

```
bbox/
├── 유증상/
│   ├── A1/_bbox/*.jpg
│   ├── A2/_bbox/*.jpg
│   ├── ...
│   └── A6/_bbox/*.jpg
└── 무증상/
    ├── A1/_bbox/*.jpg
    └── ...
```

### 2.3 데이터 분할 비율

| 세트             | 비율 |
| ---------------- | ---- |
| 학습(Train)      | 80%  |
| 검증(Validation) | 10%  |
| 테스트(Test)     | 10%  |

> 유증상과 무증상 각각 독립적으로 비율을 분리하여 계층적 분할을 수행한다.

---

## 3. 모델 아키텍처

### 3.1 기반 모델: Inception V4

모든 분류 모델의 기반 아키텍처로 **Google의 Inception V4**를 사용한다.

| 항목        | 내용                              |
| ----------- | --------------------------------- |
| 아키텍처    | Inception V4                      |
| 프레임워크  | TensorFlow 2.7.0 / Keras 2.7.0    |
| 입력 해상도 | 224 × 224 × 3 (RGB)               |
| 입력 정규화 | 픽셀값 ÷ 255 → [0, 1] 범위 정규화 |

#### Inception V4 구조 특징

- **모듈화된 블록**: Stem → Inception-A × 4 → Reduction-A → Inception-B × 7 → Reduction-B → Inception-C × 3 → Global Average Pooling → Dropout → Softmax
- **다중 스케일 처리**: 1×1, 3×3, 5×5(분해) 병렬 합성곱으로 다양한 공간적 특징 동시 추출
- **잔차 연결 없음**: Inception V4는 Inception-ResNet과 달리 순수 Inception 블록 구조

### 3.2 분류 방식

#### Binary 분류

```
출력 노드: 2개  [유증상, 무증상]
레이블 인코딩:
  - label == 7 (무증상) → [1, 0]
  - label != 7 (유증상) → [0, 1]
```

#### Multi-class 분류

```
출력 노드: 7개  [A1, A2, A3, A4, A5, A6, 무증상]
레이블 인코딩: One-Hot (label - 1 → 0-indexed)
  - A1(label=1) → [1,0,0,0,0,0,0]
  - A2(label=2) → [0,1,0,0,0,0,0]
  - ...
  - 무증상(label=7) → [0,0,0,0,0,0,1]
```

---

## 4. 고양이(Cat) 모델 분석

### 4.1 모델 구성

고양이 피부질환 분류는 현재 **이진 분류(Binary)** 모델 1종이 구현되어 있다.

| 모델명         | 분류 유형 | 대상 클래스 | 입력 경로      |
| -------------- | --------- | ----------- | -------------- |
| `cat_binary_2` | Binary    | A2          | `tfrecord_A2/` |

### 4.2 하이퍼파라미터

| 파라미터               | 값        |
| ---------------------- | --------- |
| 배치 크기 (Batch Size) | **32**    |
| 에폭 (Epoch)           | 100       |
| 이미지 크기            | 224 × 224 |
| 셔플                   | True      |

### 4.3 분류 목표

- **A2 질환 유/무 이진 분류**
- 출력: 유증상(A2 질환 있음) vs 무증상(정상)
- 테스트 클래스 ID: `['2', '7']` (A2=2, 무증상=7)

---

## 5. 강아지(Dog) 모델 분석

### 5.1 모델 구성

강아지 피부질환 분류는 **다중 분류** 모델이 구현되어 있다.

| 모델명             | 분류 유형 | 대상 클래스 | 배치 크기 | 학습률      |
| ------------------ | --------- | ----------- | --------- | ----------- |
| `dog_multi_123456` | Multi     | A1~A6 전체  | 64        | 0.000213388 |

### 5.2 Multi-class 모델: dog_multi_123456

| 파라미터    | 값                                   |
| ----------- | ------------------------------------ |
| 학습률      | **0.000213388**                      |
| 배치 크기   | **64**                               |
| 에폭        | 100                                  |
| 셔플        | True                                 |
| 대상 클래스 | A1, A2, A3, A4, A5, A6 전체 + 무증상 |

---

## 6. TFRecord 데이터 처리

### 6.1 TFRecord 스키마

각 TFRecord 레코드는 다음 필드를 포함한다:

```python
{
    'id':        int64   # 이미지 고유 ID
    'height':    int64   # 이미지 높이 (픽셀)
    'width':     int64   # 이미지 너비 (픽셀)
    'depth':     int64   # 채널 수 (3: RGB)
    'label':     int64   # 클래스 레이블 (1~7)
    'image_raw': bytes   # JPEG 원본 바이트
}
```

### 6.2 전처리 파이프라인 (`tfrecord_convert.py`)

```
TFRecord 파일 로드
      ↓
JPEG 디코딩 (RGB 3채널)
      ↓
224×224 리사이즈
      ↓
픽셀 정규화 (÷ 255)
      ↓
레이블 인코딩
  ├─ multi:  One-Hot (7차원, label-1 인덱스)
  └─ binary: [1,0] 무증상 / [0,1] 유증상
      ↓
병렬 처리 (tf.data.AUTOTUNE)
```

---

## 7. 데이터 준비 유틸리티

### 7.1 `polygon2traindata.py`

JSON 어노테이션의 폴리곤/바운딩박스 정보를 이용하여 원본 이미지에서 **피부 병변 영역을 크롭**하는 전처리 도구이다.

주요 기능:

- 폴리곤 → 마스크 이미지 변환
- 폴리곤 → 바운딩박스 좌표 추출
- 바운딩박스 기준 이미지 크롭 (`_bbox` 폴더 저장)
- 처리 이력 관리 (resume/rework 지원)
- 최소 면적 비율 필터링

### 7.2 `prepare_dog_dataset_from_152.py`

AI Hub의 "152. 반려동물 피부질환 데이터"를 프로젝트 표준 구조로 변환하는 유틸리티이다.

- 원본 경로에서 `반려견` + `피부` 조합의 JPG 파일 탐색
- A1~A6 클래스와 유증상/무증상 자동 분류
- MD5 해시 기반 중복 방지 파일명 생성
- copy / hardlink / move 3가지 배치 방식 지원

### 7.3 `make_tfrecord.py`

크롭된 이미지를 TFRecord 형식으로 변환하는 스크립트이다.

- 데이터셋 8:1:1 분할 (train/val/test)
- 유증상/무증상 계층별 독립 분할
- 1,000장 단위 TFRecord 파일 분할 저장
- CSV 메타데이터 저장 (`tfrecord_list.csv`, `splited_df.csv`)

---

## 8. 기술 스택

| 항목         | 버전   |
| ------------ | ------ |
| Python       | 3.8    |
| TensorFlow   | 2.7.0  |
| Keras        | 2.7.0  |
| NumPy        | 1.22.2 |
| Pandas       | 1.4.0  |
| Pillow       | 9.0.1  |
| scikit-learn | 1.0.2  |
